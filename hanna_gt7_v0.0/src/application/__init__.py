"""
Camada de aplicação: orquestração, estado de tela e eventos.

Depende de `domain`, nunca de `infrastructure` ou `presentation` — fala com o
mundo externo apenas através das interfaces declaradas no domínio.
"""
