# O núcleo depende só disto. O protocolo do GT7 é criptografado com Salsa20,
# e é o único motivo desta dependência existir.
pycryptodome>=3.19,<4

# Tudo o mais é OPCIONAL, e o programa funciona sem cada peça. Este arquivo
# listava `PySide6` como obrigatório, o que contradizia o `pyproject.toml` e
# faria alguém instalar 100 MB de Qt para rodar a análise na linha de comando.
#
# A fonte de verdade é o pyproject:
#
#   pip3 install -e ".[app]"        interface gráfica  (PySide6)
#   pip3 install -e ".[ai]"         provedor de nuvem  (anthropic)
#   pip3 install -e ".[discord]"    bot                (discord.py)
#   pip3 install -e ".[dev]"        pytest, ruff, mypy
#
# A IA local não entra aqui: não é pacote Python, é um servidor de modelo
# (`ollama serve`). A voz também não: usa o sintetizador do sistema.
