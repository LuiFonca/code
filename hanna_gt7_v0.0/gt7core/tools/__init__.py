"""Ferramentas de linha de comando do núcleo.

Rodam sem interface gráfica, o que é o ponto: quando o programa não mostra
dados, a primeira pergunta é se a telemetria chega — e responder isso não pode
depender de a interface subir.
"""
